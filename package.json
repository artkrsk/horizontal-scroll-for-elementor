{
  "name": "horizontal-scroll-for-elementor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=22.17.0"
  },
  "scripts": {
    "dev": "pnpm sync:fixtures && node build/index.js dev",
    "build": "node build/index.js build",
    "sync": "pnpm sync:fixtures",
    "sync:fixtures": "node dev/sync-fixtures.js",
    "blueprint:build": "node dev/blueprint/build-blueprint.js",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "phpstan": "composer phpstan",
    "test": "pnpm build && wp-env start && pnpm test:php && pnpm test:php:no-elementor",
    "test:php": "wp-env run tests-cli --env-cwd=test-workspace -- vendor/bin/phpunit",
    "test:php:no-elementor": "wp-env run tests-cli --env-cwd=test-workspace -- vendor/bin/phpunit -c phpunit-no-elementor.xml.dist"
  },
  "devDependencies": {
    "@artemsemkin/elementor-types": "^1.0.10",
    "@biomejs/biome": "2.5.4",
    "@types/node": "^26.1.1",
    "@wordpress/env": "11.11.0",
    "archiver": "^8.0.0",
    "chokidar": "^5.0.0",
    "esbuild": "0.28.1",
    "sass": "^1.101.0",
    "typescript": "^7.0.2"
  }
}
